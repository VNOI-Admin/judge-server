#include <bits/stdc++.h>
#include "header.h"

using namespace std;

string ask(long long n) {
  cout << n << endl;
  string res;
  cin >> res;
  return res;
}

int main() {
  cin.tie(0);
  cin.sync_with_stdio(0);
  solve();
  return 0;
}
